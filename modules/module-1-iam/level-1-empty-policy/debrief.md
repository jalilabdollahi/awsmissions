# Debrief

Empty statements are effectively empty permissions. In real AWS this often looks like a role that exists and appears wired correctly, but every application call still fails with `AccessDenied`.
